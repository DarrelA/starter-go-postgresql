version: '3.8'
services:
  postgres:
    image: postgres:latest
    container_name: postgres
    ports:
      - '6500:5432'
    volumes:
      - progresDB_${APP_ENV}:/var/lib/postgresql/data
      - ../db/pgdb/init.sql:/docker-entrypoint-initdb.d/init.sql
    env_file:
      - ../configs/.env.${APP_ENV}

  pgAdmin:
    image: dpage/pgadmin4
    container_name: pgAdmin
    ports:
      - '5050:80'
    volumes:
      - ../db/pgdb/servers.${APP_ENV}.json:/pgadmin4/servers.json
    env_file:
      - ../configs/.env.${APP_ENV}

  redis:
    image: redis:alpine
    container_name: redis
    ports:
      - '6379:6379'
    volumes:
      - redisDB_${APP_ENV}:/data

  app:
    build:
      context: ..
      dockerfile: ./deployments/Dockerfile
    image: starter-go-postgresql
    container_name: starter-go-postgresql
    ports:
      - '8080:8080'
    volumes:
      - ./logs:/app/logs
    environment:
      - APP_ENV=${APP_ENV}
    env_file:
      - ../configs/.env.${APP_ENV}
    depends_on:
      - postgres
      - redis

volumes:
  redisDB_development:
  progresDB_development:
  redisDB_test:
  progresDB_test:
  redisDB_production:
  progresDB_production:
